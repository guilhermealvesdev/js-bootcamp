let person = {
    name: 'Guilherme',
    age: 29,
    location: 'Santos'
}

console.log(`${person.name} tem ${person.age} anos e mora em ${person.location}`);

person.age++;

console.log(`${person.name} tem ${person.age} anos e mora em ${person.location}`);